# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: regression\error-handling.spec.ts >> Error Handling and Edge Cases Regression >> User can navigate back to home from error page
- Location: tests\regression\error-handling.spec.ts:24:7

# Error details

```
Error: page.goto: net::ERR_HTTP2_PROTOCOL_ERROR at https://www.eventim.de/this-page-does-not-exist-12345
Call log:
  - navigating to "https://www.eventim.de/this-page-does-not-exist-12345", waiting until "load"

```

# Test source

```ts
  1  | import { Page } from '@playwright/test';
  2  | import { Selectors } from '../core/selectors';
  3  | 
  4  | export class ErrorPage {
  5  |   constructor(private page: Page) {}
  6  | 
  7  |   async navigateTo404(): Promise<void> {
> 8  |     await this.page.goto('/this-page-does-not-exist-12345');
     |                     ^ Error: page.goto: net::ERR_HTTP2_PROTOCOL_ERROR at https://www.eventim.de/this-page-does-not-exist-12345
  9  |   }
  10 | 
  11 |   async getErrorCode(): Promise<string | null> {
  12 |     const errorTitle = this.page.locator('h1.error-code, [data-testid="error-code"]');
  13 |     if (await errorTitle.isVisible()) {
  14 |       return errorTitle.textContent();
  15 |     }
  16 |     return null;
  17 |   }
  18 | 
  19 |   async getErrorMessage(): Promise<string | null> {
  20 |     const errorMessage = this.page.locator('p.error-message, [data-testid="error-message"]');
  21 |     if (await errorMessage.isVisible()) {
  22 |       return errorMessage.textContent();
  23 |     }
  24 |     return null;
  25 |   }
  26 | 
  27 |   async clickGoHomeButton(): Promise<void> {
  28 |     const goHomeBtn = this.page.locator('a.go-home, [data-testid="go-home-btn"]');
  29 |     if (await goHomeBtn.isVisible()) {
  30 |       await goHomeBtn.click();
  31 |     }
  32 |   }
  33 | }
  34 | 
```